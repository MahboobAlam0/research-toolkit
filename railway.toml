[build]
builder      = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"

[deploy]
# Railway injects $PORT automatically — the Dockerfile CMD handles it
restartPolicyType    = "ON_FAILURE"
restartPolicyMaxRetries = 3
